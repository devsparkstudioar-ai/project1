# Copy to ".env" for local development.
# URL of the backend API (see /server). During local dev this is the
# Express server started with `npm run dev` inside /server.
VITE_API_URL=https://project1-inky-phi.vercel.app/
